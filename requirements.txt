# Pinned to the exact versions verified working in the v6 dry-run harness.
# Unpinned deps meant every Actions run installed whatever was latest that
# morning — a silent drift vector. Bump deliberately, not accidentally.
requests==2.34.2
supabase==2.31.0
python-dotenv==1.2.2
tenacity==9.1.4
beautifulsoup4==4.15.0
